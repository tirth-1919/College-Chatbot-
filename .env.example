# ==============================================================================
# AI-POWERED COLLEGES CHATBOT - ENVIRONMENT CONFIGURATION TEMPLATE
# Permanent multi-college platform identity — never renamed per tenant
# ==============================================================================

# Core Application
ENVIRONMENT=development
DEBUG=true
APP_NAME=AI-Powered Colleges Chatbot
APP_VERSION=1.0.0

# Security & Secrets
# Generate a secure 64-char key in production: openssl rand -hex 32
SECRET_KEY=ait-ai-assistant-enterprise-secret-key-replace-in-production-2026

# Database Architecture
# Development: SQLite (zero configuration out of the box)
DATABASE_URL=sqlite:///./backend/ait_assistant.db
# Production: PostgreSQL with pgvector
# DATABASE_URL=postgresql+psycopg2://ait_user:ait_secure_password@localhost:5432/ait_assistant

# Redis Cache & State (graceful in-memory fallback if disabled or unreachable)
USE_REDIS=false
REDIS_URL=redis://localhost:6379/0

# AI Provider Credentials (Graceful fallback to verified AIT database/RAG if keys absent)
GEMINI_API_KEY=
# Ordered Gemini candidates. Rotation occurs only for quota/rate-limit errors.
GEMINI_MODELS=gemini-3.7-flash,gemini-3.8-flash,gemini-3.6-flash,gemini-3.5-flash,gemini-flash-latest,gemini-3.5-flash-lite,gemini-3.1-flash-lite,gemini-flash-lite-latest,gemini-2.5-flash,gemini-2.5-flash-lite
GEMINI_MODEL_COOLDOWN_SECONDS=300
OPENAI_API_KEY=

# Google OAuth Credentials (Graceful UI configuration notification if empty)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Storage Limits
MAX_FILE_SIZE_BYTES=26214400
UPLOAD_DIR=./backend/storage/uploads
IMAGE_STORAGE_DIR=./backend/storage/images
