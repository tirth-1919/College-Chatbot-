services:
  - type: web
    name: ai-faq-college-chat-bot
    runtime: python
    buildCommand: cd apps/user-web && npm install && npm run build && cd ../.. && pip install -r backend/requirements.txt
    startCommand: python -m uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT
